#ifndef  HELPER_INC
#define  HELPER_INC

// no namespace, added this back to have some basic functions to cut out some redundancy
bool is_between(int low, int in, int high);

#endif   /* ----- #ifndef HELPER_INC  ----- */
