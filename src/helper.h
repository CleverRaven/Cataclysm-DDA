#ifndef HELPER_H
#define HELPER_H

#include <string>
#include <sstream>

namespace helper
{
int to_int(std::string str);
std::string to_string_int(int i);
std::string to_string_long(long i);
}

#endif
